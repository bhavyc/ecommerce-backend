<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Admin Dashboard</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">
  <style>
    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background: linear-gradient(135deg, #d4f7d4, #a3e4a3);
      margin: 0;
      overflow-x: hidden;
    }

    /* Sidebar */
    #sidebar {
      position: fixed;
      left: 0;
      top: 0;
      width: 220px;
      height: 100vh;
      background: rgba(46, 125, 50, 0.95);
      backdrop-filter: blur(8px);
      padding-top: 60px;
      transition: 0.3s;
      z-index: 1000;
    }
    #sidebar h4 {
      color: white;
      text-align: center;
      margin-bottom: 30px;
      font-weight: 700;
      letter-spacing: 1px;
    }
    #sidebar a {
      display: flex;
      align-items: center;
      gap: 12px;
      padding: 12px 20px;
      color: white;
      font-weight: 500;
      text-decoration: none;
      border-radius: 10px;
      margin: 8px 10px;
      transition: all 0.3s;
      position: relative;
    }
    #sidebar a::before {
      content: '';
      position: absolute;
      left: 0;
      height: 100%;
      width: 5px;
      background: #a5d6a7;
      border-radius: 5px 0 0 5px;
      transform: scaleY(0);
      transform-origin: top;
      transition: transform 0.3s;
    }
    #sidebar a:hover::before {
      transform: scaleY(1);
    }
    #sidebar a:hover {
      transform: translateX(8px);
      background: rgba(27, 94, 32, 0.85);
    }

    /* Main content */
    #main-content {
      margin-left: 240px;
      padding: 30px;
      transition: 0.3s;
    }
    .top-nav {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: 30px;
    }
    .top-nav h1 {
      color: #1b5e20;
      font-size: 28px;
      font-weight: 700;
      text-shadow: 1px 1px 4px rgba(0,0,0,0.1);
    }
    .top-nav span {
      font-weight: 600;
      color: #2e7d32;
      background: rgba(255,255,255,0.6);
      padding: 6px 14px;
      border-radius: 12px;
      box-shadow: 0 2px 6px rgba(0,0,0,0.1);
    }

    /* Cards */
    .card {
      background: rgba(255,255,255,0.8);
      backdrop-filter: blur(6px);
      border-radius: 18px;
      padding: 30px;
      box-shadow: 0px 8px 25px rgba(0,128,0,0.15);
      transition: transform 0.4s, box-shadow 0.4s;
      text-align: center;
    }
    .card:hover {
      transform: translateY(-12px);
      box-shadow: 0px 15px 35px rgba(0,128,0,0.25);
    }
    .card h3 {
      color: #1b5e20;
      margin-bottom: 15px;
    }
    .card p {
      font-size: 14px;
      color: #2e7d32;
      margin-bottom: 20px;
    }
    .btn-primary {
      background: #2e7d32;
      border: none;
      transition: all 0.3s;
    }
    .btn-primary:hover {
      background: #1b5e20;
      transform: scale(1.08);
    }

    .icon { font-size: 22px; }

    @media (max-width: 768px) {
      #sidebar { width: 60px; padding-top: 20px; }
      #sidebar a { justify-content: center; gap: 0; padding: 12px 0; font-size: 12px; }
      #main-content { margin-left: 70px; padding: 20px; }
      .top-nav h1 { font-size: 20px; }
      .card { padding: 20px; }
    }
  </style>
</head>
<body>

<!-- Sidebar -->
<div id="sidebar">
  <h4>Admin</h4>
  <a href="/admin/dashboard"><i class="fas fa-home icon"></i> Dashboard</a>
  <a href="/admin/drops"><i class="fas fa-apple-alt icon"></i> Drops List</a>
  <a href="/admin/deals"><i class="fas fa-bolt icon"></i> 24-Hour Deals</a>
  <a href="/admin/sellers"><i class="fas fa-store icon"></i> Sellers</a>
  <a href="http://localhost:5000/admin/sellers/pending"><i class="fas fa-clock icon"></i> Pending Sellers</a> <!-- ✅ Added -->
  <a href="/admin/register"><i class="fas fa-user-plus icon"></i> Create Admin</a>
  <a href="/admin/logout"><i class="fas fa-sign-out-alt icon"></i> Logout</a>
</div>

<!-- Main Content -->
<div id="main-content"> 
  <div class="top-nav">
    <h1>Welcome, <%= user.name %>!</h1>
    <span>Admin Panel</span>
  </div>

  <div class="row g-4">
    <div class="col-md-6 col-lg-4">
      <div class="card">
        <h3><i class="fas fa-apple-alt icon"></i> Manage Drops</h3>
        <p>Create, update, or view all fruit drops here.</p>
        <a href="/admin/drops" class="btn btn-primary">Go to Drops List</a>
      </div>
    </div>

    <div class="col-md-6 col-lg-4">
      <div class="card">
        <h3><i class="fas fa-user-plus icon"></i> Create Admin</h3>
        <p>Add new admin users to manage the system.</p>
        <a href="/admin/register" class="btn btn-primary">Create Admin</a>
      </div>
    </div>

    <div class="col-md-6 col-lg-4">
      <div class="card">
        <h3><i class="fas fa-user icon"></i> Profile</h3>
        <p>View your account details and settings.</p>
        <span class="fw-bold text-success"><%= user.email %></span>
      </div>
    </div>

    <!-- ✅ Optional Pending Sellers Card -->
    <div class="col-md-6 col-lg-4">
      <div class="card">
        <h3><i class="fas fa-clock icon"></i> Pending Sellers</h3>
        <p>Review and approve pending seller applications.</p>
        <a href="http://localhost:5000/admin/sellers/pending" class="btn btn-primary">View Pending Sellers</a>
      </div>
    </div>
  </div>
</div>

<!-- Bootstrap JS -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
