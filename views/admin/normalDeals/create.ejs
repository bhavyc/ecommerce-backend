<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Create Normal Deal</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      background: linear-gradient(135deg, #d0f0c0, #a8e6cf);
      min-height: 100vh;
      font-family: 'Segoe UI', sans-serif;
    }

    h2 {
      text-align: center;
      margin-bottom: 30px;
      color: #1b5e20;
      font-weight: 700;
      text-shadow: 1px 1px 2px rgba(0,0,0,0.1);
    }

    .form-container {
      background: #ffffffdd;
      padding: 30px;
      border-radius: 15px;
      box-shadow: 0 10px 25px rgba(0,0,0,0.1);
      max-width: 600px;
      margin: auto;
      transition: transform 0.3s, box-shadow 0.3s;
    }

    .form-container:hover {
      transform: translateY(-5px);
      box-shadow: 0 15px 30px rgba(0,0,0,0.15);
    }

    .form-control, .form-select {
      border-radius: 10px;
      transition: 0.3s;
    }

    .form-control:focus, .form-select:focus {
      border-color: #43a047;
      box-shadow: 0 0 8px rgba(67,160,71,0.3);
    }

    .btn-primary {
      background-color: #43a047;
      border-radius: 50px;
      transition: background 0.3s, transform 0.2s, box-shadow 0.2s;
      font-weight: 600;
    }

    .btn-primary:hover {
      background-color: #388e3c;
      transform: translateY(-2px);
      box-shadow: 0 5px 15px rgba(0,0,0,0.2);
    }

    .btn-secondary {
      border-radius: 50px;
      transition: transform 0.2s, box-shadow 0.2s;
    }

    .btn-secondary:hover {
      transform: translateY(-2px);
      box-shadow: 0 5px 15px rgba(0,0,0,0.15);
    }

    .form-check-label {
      margin-left: 5px;
      font-weight: 500;
    }
  </style>
</head>
<body class="mt-5">

  <div class="form-container">
    <h2>Create New Normal Deal</h2>

    <% if (error) { %>
      <div class="alert alert-danger"><%= error %></div>
    <% } %>

    <!-- 1. Form ka ACTION URL theek kiya gaya hai -->
    <form action="/admin/normal-deals" method="POST">

      <!-- 2. Sabse zaroori badlaav: Product Select karne ka Dropdown -->
      <div class="mb-3">
        <label class="form-label">Associated Product</label>
        <select name="productId" class="form-select" required>
            <option value="" disabled selected>-- Select a Product --</option>
            <% if (products && products.length > 0) { %>
                <% products.forEach(product => { %>
                    <option value="<%= product._id %>"><%= product.title %></option>
                <% }) %>
            <% } else { %>
                <option value="" disabled>No products found. Please create a product first.</option>
            <% } %>
        </select>
        <div class="form-text">Har deal ko ek product se link karna zaroori hai.</div>
      </div>

      <div class="mb-3">
        <label class="form-label">Title</label>
        <input type="text" name="title" class="form-control" placeholder="Enter deal title" required>
      </div>

      <div class="mb-3">
        <label class="form-label">Description</label>
        <textarea name="description" class="form-control" rows="3" placeholder="Enter deal description" required></textarea>
      </div>

      <div class="mb-3">
        <label class="form-label">Price (₹)</label>
        <input type="number" name="price" class="form-control" step="0.01" placeholder="Enter price" required>
      </div>

      <div class="mb-3">
        <label class="form-label">Image URL</label>
        <input type="text" name="image" class="form-control" placeholder="Enter image link">
      </div>

      <div class="mb-3 form-check">
        <input type="checkbox" name="featured" class="form-check-input" id="featuredCheck">
        <label class="form-check-label" for="featuredCheck">Featured Deal</label>
      </div>

      <button type="submit" class="btn btn-primary w-100 mb-3">Create Deal</button>
      <a href="/admin/normal-deals" class="btn btn-secondary w-100">Back to List</a>
    </form>
  </div>

</body>
</html> 
